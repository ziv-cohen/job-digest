[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Railway cron: run daily at 8:00 AM CEST (UTC+2) = 6:00 AM UTC
cronSchedule = "0 6 * * *"
restartPolicyType = "NEVER"

[deploy.healthcheck]
# No healthcheck needed — this is a cron job, not a web server
enabled = false
